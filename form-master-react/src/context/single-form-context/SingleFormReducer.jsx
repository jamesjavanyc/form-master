const SingleFormReducer = (state ,action)=>{
    switch(action.type){
        case "CREATE":
            return{

            };
        case "OPERATE":
            return{

            };
        default:
            return state;
    }
};

export default SingleFormReducer;